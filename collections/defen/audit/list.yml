info:
  name: list
  type: http
  seq: 1

http:
  method: GET
  url: "{{url}}/{{version}}/organizations/audit-logs"
  headers:
    - name: Content-Type
      value: application/json
      disabled: true
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
