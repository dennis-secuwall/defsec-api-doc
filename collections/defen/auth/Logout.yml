info:
  name: Logout
  type: http
  seq: 8

http:
  method: POST
  url: "{{url}}/{{version}}/auth/logout"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
