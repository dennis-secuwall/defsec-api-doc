info:
  name: Admin-triggered password reset
  type: http
  seq: 6

http:
  method: POST
  url: "{{url}}/{{version}}/auth/admin-reset-password"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "user_id": "019d5704-71d9-70e9-9a8c-810faba1e737",
        "password": "Password123!1"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
