info:
  name: Ban a user
  type: http
  seq: 7

http:
  method: POST
  url: "{{url}}/{{version}}/auth/ban"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "user_id": "019d66bd-b6c6-7592-9cd4-96eac009ad10",
        "duration": 1
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
