info:
  name: update
  type: http
  seq: 4

http:
  method: PUT
  url: "{{url}}/{{version}}/users/me"
  body:
    type: json
    data: |-
      {
          "first_name" : "Dennis 1",
          "last_name": " Huynh"
      }
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
