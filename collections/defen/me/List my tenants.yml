info:
  name: List my tenants
  type: http
  seq: 2

http:
  method: GET
  url: "{{url}}/{{version}}/users/me/tenants"
  auth:
    type: bearer
    token: "{{access_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
